# Material Management System

This project is a full-stack web application consisting of:
- MySQL database running in Docker
- Node.js backend providing RESTful APIs
- React.js frontend consuming backend APIs

The project focuses on building stable and reusable foundational features first,
while iteratively refining the system design based on working results.
